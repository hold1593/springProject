<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="memberMapper">
	<!-- 
		1) mybatis-config.xml에 typeAilas, mapper 등록
		2) resultMap 만들기
		3) 쿼리문 작성
	 -->
	<resultMap type="Member" id="memberResultSet">
		<id property="memberNo" column="MEMBER_NO"/>
		<result property="memberId" column="MEMBER_ID"/>
  		<result property="memberPwd" column="MEMBER_PWD"/>
  		<result property="memberName" column="MEMBER_NM"/>
  		<result property="memberPhone" column="MEMBER_PHONE"/>
  		<result property="memberEmail" column="MEMBER_EMAIL"/>
  		<result property="memberAddress" column="MEMBER_ADDR"/>
  		<result property="memberInterest" column="MEMBER_INTEREST"/>
  		<result property="memberEnrollDate" column="MEMBER_ENROLL_DT"/>
  		<result property="memberStatus" column="MEMBER_STATUS"/>
  		<result property="memberGrade" column="MEMBER_GRADE"/>
	</resultMap>
	 
	 
	 <!-- 로그인 -->
	<select id="loginMember" parameterType="Member" resultMap="memberResultSet">
		SELECT * FROM MEMBER
		WHERE MEMBER_ID = #{memberId}
	<!-- 	AND MEMBER_PWD = #{memberPwd} -->
		AND MEMBER_STATUS='Y'
	</select>
	
	<!-- 회원가입 -->
	<insert id="signUp" parameterType="Member">
		INSERT INTO MEMBER
		VALUES(SEQ_UNO.NEXTVAL, #{memberId}, #{memberPwd},
				#{memberName}, #{memberPhone}, #{memberEmail},
				#{memberAddress}, #{memberInterest},
				DEFAULT,DEFAULT,DEFAULT)
	</insert>
	
	<!-- 아이디 중복 검사 -->
	<select id="idDupCheck" parameterType="string" resultType="_int">
		SELECT COUNT(*) FROM MEMBER
		WHERE MEMBER_ID = #{memberId}
	</select>
	
	<select id="selectMember" parameterType="_int" resultMap="memberResultSet">
		SELECT * FROM MEMBER 
		WHERE MEMBER_NO=#{memberNo}
		AND MEMBER_STATUS='Y'
	</select>
	
	<update id="updateMember" parameterType="Member">
		UPDATE MEMBER SET MEMBER_PHONE=#{memberPhone}, MEMBER_EMAIL=#{memberEmail},
		 MEMBER_ADDR=#{memberAddress}, MEMBER_INTEREST=#{memberInterest} 
		 WHERE MEMBER_ID=#{memberId}
	</update>
	
	<!--비밀번호 조회 -->
	<select id="selectPwd" parameterType="_int" resultType="string">
		SELECT MEMBER_PWD FROM MEMBER
		WHERE MEMBER_NO=#{memberNo}
	</select>
	
	<!-- 비밀번호 수정 -->
	<update id="updatePwd" parameterType="Member">
		UPDATE MEMBER SET 
		MEMBER_PWD=#{memberPwd}
		WHERE MEMBER_NO=#{memberNo}
	</update>

</mapper>
